import { Project } from '../';
import * as ζbuild from './build';
import * as ζserve from './serve';
export declare class CustomProject extends Project {
    readonly type: 'custom';
    /**
     * We can't detect custom project types. We don't know what they look like!
     */
    detected(): Promise<boolean>;
    requireBuildRunner(): Promise<ζbuild.CustomBuildRunner>;
    requireServeRunner(): Promise<ζserve.CustomServeRunner>;
    requireGenerateRunner(): Promise<never>;
}
